//> using scala "3.7.1"
//> using options "-deprecation" "-feature" "-unchecked" "-Wunused:all" "-Wvalue-discard" "-Wnonunit-statement"
//> using test.dep "org.scalameta::munit::1.1.1"

/** Build definition for the learning SQLite implementation.
  *
  * Scala CLI keeps the repository approachable: every source under `src` and
  * every test under `test` is compiled by `scala-cli test .`.
  */
object Project
